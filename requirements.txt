# Pinned to the versions used to produce the results in the paper.
# Python 3.9. TensorFlow 2.12 ships Keras 2 natively (no tf-keras needed).
# GPU: TF 2.12 uses the GPU automatically on Linux given a matching system
# CUDA 11.8 / cuDNN 8.6 install (see the TensorFlow 2.12 GPU requirements).

# Core numerical / ML
numpy==1.23.5
tensorflow==2.12.0
tensorflow-probability==0.20.1
keras==2.12.0

# Scientific computing
scipy==1.12.0
scikit-learn==1.2.2
numba==0.59.1

# Plotting
matplotlib==3.7.1

# Utilities
tqdm==4.65.0
ipywidgets==8.0.6

# Notebook runtime (to open and run RSMI_*.ipynb). Left unpinned: any recent
# Jupyter works; pip resolves a build compatible with the pins above.
notebook
jupyterlab
